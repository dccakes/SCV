import { sharedStyles } from '~/app/utils/shared-styles'

export default function Footer() {
  return (
    <footer className={`mt-32 bg-black text-white ${sharedStyles.desktopPaddingSides}`}>
      Footer
    </footer>
  )
}
