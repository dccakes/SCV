# Docker Compose for OSWP
#
# Usage:
#   Development (PostgreSQL only, run app with npm run dev):
#     docker compose up -d
#
#   Production (full stack):
#     docker compose --profile prod up -d
#
#   With pgAdmin (database GUI):
#     docker compose --profile admin up -d
#
#   Everything:
#     docker compose --profile prod --profile admin up -d

services:
  # ==========================================
  # PostgreSQL Database (always runs)
  # ==========================================
  postgres:
    image: postgres:16-alpine
    container_name: oswp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-oswp_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-oswp_password}
      POSTGRES_DB: ${POSTGRES_DB:-oswp_db}
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-oswp_user} -d ${POSTGRES_DB:-oswp_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - oswp-network

  # ==========================================
  # Application (production profile)
  # ==========================================
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: oswp-app
    restart: unless-stopped
    profiles:
      - prod
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://${POSTGRES_USER:-oswp_user}:${POSTGRES_PASSWORD:-oswp_password}@postgres:5432/${POSTGRES_DB:-oswp_db}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET:-change-me-in-production}
      - BETTER_AUTH_URL=${BETTER_AUTH_URL:-http://localhost:3000}
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}
      - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL:-}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - oswp-network

  # ==========================================
  # Database Admin (pgAdmin)
  # ==========================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: oswp-pgadmin
    restart: unless-stopped
    profiles:
      - admin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@example.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres
    networks:
      - oswp-network

networks:
  oswp-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  pgadmin_data:
    driver: local
